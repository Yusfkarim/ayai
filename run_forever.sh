#!/bin/bash
# ════════════════════════════════════════════
#  چاودێری بۆت — ئەگەر بوەستێت، دەیگەڕێنێتەوە
# ════════════════════════════════════════════
cd "$(dirname "$0")"

while true; do
  echo "[KEEPALIVE] 🚀 دەستپێکردنی بۆت — $(date '+%H:%M:%S')"
  python3 -u main.py
  code=$?
  echo "[KEEPALIVE] ⚠️ بۆتەکە وەستا (کۆد: $code) — ٥ چرکەی تر دووبارە دەستپێدەکاتەوە…"
  sleep 5
done
